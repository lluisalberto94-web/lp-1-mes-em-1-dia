import Link from 'next/link';
import { createIdea } from '@/app/actions';
import { prisma } from '@/lib/db';
import { authorLabel, enumValues, pillarLabel } from '@/lib/meta';
export const dynamic='force-dynamic';
export default async function Ideas(){const ideas=await prisma.idea.findMany({orderBy:{createdAt:'desc'}});return <div className="content"><div className="page-head"><div><div className="eyebrow">Captura rápida</div><h1>Banco de Ideias</h1><p>Ideia incompleta é permitida. O objetivo aqui é não perder matéria-prima.</p></div></div><div className="idea-grid"><form className="panel" action={createIdea}><div className="form-group"><label>Título / ideia</label><input className="field" name="title" required placeholder="Empresário que confunde saldo com lucro"/></div><div className="form-group mt-4"><label>Observação</label><textarea className="textarea" name="notes"/></div><div className="form-group mt-4"><label>Possível autor</label><select className="select" name="author"><option value="">Em aberto</option>{enumValues.authors.map(a=><option value={a} key={a}>{authorLabel[a]}</option>)}</select></div><div className="form-group mt-4"><label>Possível pilar</label><select className="select" name="pillar"><option value="">Em aberto</option>{enumValues.pillars.map(p=><option value={p} key={p}>{pillarLabel[p]}</option>)}</select></div><button className="button mt-5">+ Nova ideia</button></form><section className="ideas">{ideas.map(i=><article className="idea-card" key={i.id}><div className="flex justify-between gap-3"><span className="pill">{i.author?authorLabel[i.author]:'Autor aberto'}</span>{i.pillar&&<span className="pill pill-gold">{pillarLabel[i.pillar]}</span>}</div><h3>{i.title}</h3>{i.notes&&<p className="small muted">{i.notes}</p>}<Link className="button secondary mt-3" href={`/gerador?idea=${encodeURIComponent(i.title)}&ideaId=${i.id}`}>Transformar em conteúdo</Link></article>)}{!ideas.length&&<div className="empty">Nenhuma ideia ainda.</div>}</section></div></div>}
