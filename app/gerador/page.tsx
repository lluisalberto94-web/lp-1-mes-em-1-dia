import { Generator } from '@/components/Generator';
export default async function GeneratorPage({searchParams}:{searchParams:Promise<{idea?:string,ideaId?:string}>}){const params=await searchParams;return <div className="content"><div className="page-head"><div><div className="eyebrow">Gerador</div><h1>Da ideia ao ativo.</h1><p>O fluxo funciona sem API externa e já deixa o ponto de integração preparado para LLM.</p></div></div><Generator initialIdea={params.idea||''} ideaId={params.ideaId}/></div>}
